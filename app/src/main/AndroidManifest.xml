<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user.waimai">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ShopCarStartActivity"
        android:allowBackup="true"
        android:icon="@drawable/tubiao"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ShowDishActivity"
            android:label="订单详情" />
        <activity android:name=".SplashActivity" />
        <activity
            android:name=".ShowShopActivity"
            android:label="店家推荐" />
        <activity
            android:name=".ShopCarActivity"
            android:label="购物车" />
        <activity
            android:name=".ShowOrderActivity"
            android:label="订单" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Splash_loadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistActivity" />
        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden|adjustPan" />
        <activity
            android:name=".MyActivity"
            android:label="我的" />
        <activity android:name=".FindActivity"
            android:label="发现"></activity>
    </application>

</manifest>